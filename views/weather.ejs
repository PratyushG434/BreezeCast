<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Baloo+Bhai+2:wght@400..800&family=Baloo+Bhaijaan+2:wght@400..800&family=DM+Serif+Display:ital@0;1&family=DM+Serif+Text:ital@0;1&family=Funnel+Sans:ital,wght@0,300..800;1,300..800&family=Genos:ital,wght@0,100..900;1,100..900&family=Homemade+Apple&family=Lexend:wght@100..900&family=Lora:ital,wght@0,400..700;1,400..700&family=Poetsen+One&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="styles/weather.css">
    <title>BreezeCast</title>
    <link rel="icon" href="logo/BreezeCast.png" type="image/png">
</head>
<body>
    <header>
        <div class="header">
            <div class="logo">
                <img src="images/BreezeCast.png" alt="logo" height="50">

            </div>
            <form action="/submit" method="POST" class="form-class">
                <input class="search-bar" type="text" name="prompt" placeholder="Enter any location" required>
                <button class="btn" type="submit">Search</button>
            </form>
        </div>
    </header>
    <main>
    <div class="container">
        <div class="current card">
            <div id="date"><%= current.date %></div>
            <div id="location">
                <h1><%= `${current.location},${current.country}`%> </h1>
            </div>
            <div id="heading"> <h2>Current conditions </h2></div>
            <div id="temperature">
                <h1> <%= current.temp %>°C </h2>
                <h3> <%= `${current.temp_max}°C/${current.temp_min}°C` %>  </h3>
            </div>
            <div id="icon">
                <img src="images/<%= current.icon %>" alt="" height="auto" width="100%" >
            </div>
            <div id="conditions">
                <p>Feels Like : <strong><%=current.feels_like%> °C</strong></p>
                <p>Humidity : <strong><%=current.humidity%> % </strong></p>
                <p>Wind Speed : <strong><%=current.windSpeed %> Km/h</strong></p>
                <p>Pressure : <strong><%=current.pressure %> mb</strong></p>
            </div>
            <div id="description">
                <h3> <%= current.weather %></h3>
                <p> <%= current.description %> </p>

            </div>
        </div>
        <%if(locals.forecast){ 
            
        for(var i=0;i<5;i++){  %>
        <div class="forecast card-forecast card">
            <p><%= forecast[i].date %></p>
            <div class="icon-div">

                <span id="img-span"><img src='images/<%= forecast[i].icon %>' alt="Weather Icon" height="50" color="white"></span>
            </div>
            <h2> <%= forecast[i].temp_max %>°C/<%= forecast[i].temp_min %>°C</h2>
            <h4 id="forecast-description"> <%= forecast[i].weather %></h4>
        </div>
        <% } }%>
       
    </div>
    </main>
</body>
</html>